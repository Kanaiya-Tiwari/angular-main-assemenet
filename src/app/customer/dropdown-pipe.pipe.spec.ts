import { DropdownPipePipe } from './dropdown-pipe.pipe';

describe('DropdownPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownPipePipe();
    expect(pipe).toBeTruthy();
  });
});
